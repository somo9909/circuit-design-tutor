# This app uses browser-side API key entry — no .env needed.
# The Anthropic API key is entered directly in the UI at runtime.
# Never commit a real API key to version control.

# For reference only:
# VITE_ANTHROPIC_API_KEY=sk-ant-api03-...
